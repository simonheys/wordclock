{
  "name": "wordclock",
  "version": "1.1.5",
  "private": true,
  "author": "Simon Heys <si@simonheys.com>",
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "prettier": "prettier --write './**/*.{js,json,css,scss,yml,ts,tsx,md}'",
    "build": "pnpm run -r --parallel --stream build",
    "test": "CI=true pnpm run --parallel --stream test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.2.3",
    "prettier": "^3.0.1"
  },
  "jest": {
    "verbose": true
  },
  "lint-staged": {
    "*.{js,json,css,scss,yml,ts,ts,md}": "prettier --write"
  }
}
