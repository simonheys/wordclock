---
'@wordclock/canvas': minor
'@wordclock/react': minor
'@wordclock/words': patch
---

Publish the packages under the `@wordclock` namespace, add the framework-free
Canvas renderer with stable rotary phrase fitting, and render the React component
through that engine while preserving CSS-driven theme colours. Fit linear rows
against the CSS em so Canvas density matches the previous DOM renderer.
